- name: Prepare Debian-based hosts for Ansible management
  hosts: infra
  become: yes
  gather_facts: no

  tasks:
    - name: Install python3-apt for Ansible
      ansible.builtin.apt:
        name: python3-apt
        state: present
        update_cache: yes
